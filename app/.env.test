# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Rate limiter: high default so regular functional tests never hit it;
# RateLimitTest lowers the limit via $_ENV for its own scenario.
RATE_LIMIT_CONTACT_LIMIT=1000
RATE_LIMIT_CONTACT_INTERVAL='1 minute'
